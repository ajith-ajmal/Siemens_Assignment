name: My workflow
on: push
jobs:
  do-something:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./actions/
      - name: powershell
        run: |
           Out-File -FilePath EmptyFile2.txt
           git --version
           get-childitem
        shell: pwsh
      
      
